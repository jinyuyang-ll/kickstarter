# 分批采集内测记录

## G13 反馈修复回归（2026-09-11）

本轮运行 `python -m unittest test_url_collector test_collection_workflow test_request_control -q`，**47 项测试全部通过**。相比原 34 项增加了 13 项针对性测试：

- 正常项目 JSON 中的全部四种旧验证关键词不会误判。
- 403、429、HTML 验证和响应异常分别识别，验证/限流不自动重试。
- 错误的 HTTP 状态与重试信息写入摘要及断点。
- 两次单页运行共用持久化时间状态，第二次等待剩余间隔；已经过去的间隔不会重复等待。
- `Retry-After` 秒数和 HTTP 日期解析、冷却时间跨实例保存，冷却期间实际网络调用次数保持不变。
- 12 条 2026 年项目被 2014～2025 年筛选排除时计数正确，下一页仍正常推进。
- 无效 URL、年份缺失、重复及页内剩余条目分别计数。
- 原 G13 查询标识保持不变，可继续使用上一版分批断点。

时间相关测试使用可控时钟和临时 SQLite 文件，项目响应使用模拟数据；不人为向官网制造 403/429。另用本地无头 Chromium 验证默认间隔、年份跳过说明、HTTP 状态、服务端重试时间与手机布局，无 JavaScript 错误。截图保存在本机 `artifacts/g13_diagnostics.png`。

本轮不重新执行真实网站批量采集，不写 MySQL。以下 2026-09-08 的接口实测属于历史记录，不代表当前访问状态；本次也不能追溯确认学弟原始 `security_challenge` 究竟属于哪种情况。

---

日期：2026-09-08。环境：Windows，Python 3.12.7；测试使用本机已有 `.deps`，不修改数据库。公开仓库不包含本地真实配置、接口采集结果或浏览器截图。

## 离线回归

```powershell
python -m unittest test_url_collector test_collection_workflow -v
```

**34 项测试全部通过**，覆盖：

- 表单覆盖链接参数；空值清除条件；未指定命令行参数继承链接。
- 金额和比例的零值、开放上限、非有限值、负数、反向范围校验。
- 多状态拆成独立查询；重复条件组去重；不同范围分别保存断点。
- 当前页内达到数量上限后保存实际剩余记录；重启后无须重新请求即可处理剩余条目。
- 刚好处理完一页时正确进入下一页；完成查询续跑不再请求。
- 改变年份拒绝复用旧断点；CSV 丢失不静默跳过页面。
- 200 页成功后 201 页 404 标为“疑似分页受限”；第一页 404 标为请求失败。
- 空响应仍声称有下一页、缺字段和异常结构均不报告完成。
- 403 等请求错误暂停后续批次，已获取 URL 保留。
- 汇总跨批次 URL 去重；未受管理的已有 CSV 不被覆盖。
- 控制台拒绝并发采集、处理子进程启动失败、解析摘要并显示部分完成状态。
- 本地 HTTP 预览、任务创建、地区返回和非法输入响应。

所有项目 Python 文件的语法检查通过；`git diff --check` 通过。

## 官方接口小样本实测

没有执行 200 页连续抓取，仅直接请求两个边界页，并以小样本验证筛选。请求低频执行。

| 查询 | HTTP | 观察 |
| --- | --- | --- |
| Tabletop Games + Successful + Newest，第 200 页 | 200 | 12 条，`has_more=true`，`total_hits=43136` |
| 完全相同条件，第 201 页 | 404 | 本次复现分页边界现象；不据此断言所有查询永久固定为 200 页 |
| 同类别/状态 + 目标 USD 1000–5000 + 已筹 USD 1000–20000 + 比例 100–400%，第 1 页 | 200 | 12 条，`total_hits=6849` |
| 再加 United States，第 1 页 | 200 | 12 条，`total_hits=4024`；12 条均为美国项目，样本金额和比例均在范围内 |
| 官方地区 GraphQL 查询 `United States` | 200 | 返回地区名称及 ID，可解码为 `23424977` |

这些计数是本次响应中的搜索结果数，不是已经采集的数量，且会随网站数据变化。4,024 条仍可能超出单查询可翻页范围，需进一步细分。金额参数与百分比参数来自官方当前页面加载的 `advanced-discover` 前端脚本：`goal_min/max`、`pledged_min/max`、`raised_min/max`；地区使用 `woe_id`。

## 浏览器与真实任务内测

使用独立本地端口和无头 Chromium；未写 MySQL。

1. 从真实搜索链接读取类别、地区和三个范围，控件值正确。
2. 添加 Successful / Failed 两个状态，后端预览生成两个独立查询。
3. 刷新页面后条件队列和输出位置保留。
4. 实际每批运行 1 页、最多新增 3 条；界面显示待继续、当前页仍为 1。
5. 再运行相同计划，从保存的页内记录继续，汇总变为 **6 个唯一 URL**；已结束批次跳过。
6. 桌面 1440 宽与手机 390 宽检查通过，无 JavaScript 错误和横向溢出。
7. 真实地区搜索从浏览器经本地 HTTP 后台调用官方接口，返回并正确选择 United States（23424977）。

原始内测结果和截图保存在本机 `artifacts/`（已加入 Git 忽略规则）。

## 验证范围

本次验证证明筛选接入、分批、断点和去重流程可用；没有执行多年全量采集，也没有穷尽所有币种换算、区间精确边界或动态搜索结果变化。Newest 页码搜索本身在新项目插入时可能漂移；页内缓存修复了程序的截断漏项，但不能保证外部索引绝不变化。

原有其他爬虫、代理可用性和 MySQL 写入没有进行真实回归，业务逻辑未在本次修改。旧虚拟环境引用其他机器路径，内测使用本机 Python 加现有依赖目录；收件人在自己的机器按 README 安装依赖。
