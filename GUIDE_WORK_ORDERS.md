# 工单队列、自动拆分与全流程指南

## 系统结构

一个工单保存一套筛选条件、多个查询批次、定时设置和流水线选项。可以建立多个工单，系统按队列顺序一次只运行一个网络任务。工单和计划保存在本机 `.collection/`，控制台重启后会把中断的运行中工单恢复为排队状态。

流水线顺序：

1. 预检每个选中批次的第1页并读取 `total_hits`。
2. 预计页数超过阈值时，自动拆分目标金额、已筹金额或筹款比例。
3. 逐批获取项目URL，保存独立断点并汇总去重。
4. URL批次全部完成后，可自动初始化数据库、导入URL并采集项目元数据。
5. 每轮都生成Excel；URL未完成时Excel包含当前URL和批次状态，完整流水线完成后还会包含已入库元数据。

## 建立工单

在控制台设置条件组并预览批次，然后填写：

- **工单名称**：用于队列识别。
- **定时启动**：留空立即排队；填写后在本机时间到达时启动。
- **未完成重跑间隔**：本轮达到页数上限或请求失败后，按1、6、12或24小时重新排队。选择0时停在“待续跑”。
- **自动拆分页数阈值**：默认180页，给网站约200页的结果窗口留出余量。
- **自动分析并拆分**：建议开启。
- **URL完成后自动入库并采集元数据**：需要正确的 `config.json` 和MySQL。
- **本轮元数据上限**：0表示按本工单当前URL数量处理。
- **Excel输出**：必须是项目目录内的 `.xlsx` 文件。

点击“建立工单并排队”。工单下会显示当前绑定批次、下一页、预计页数和完成状态。

## 自动处理约200页结果窗口

预检按每页12条估算页数。默认阈值180页，即 `total_hits > 2160` 时拆分。拆分顺序为：

1. 目标金额 `goal`
2. 已筹金额 `pledged`
3. 已筹比例 `raised`

有限区间按中点拆成两个子区间；开放区间使用内置金额档位逐层拆分。边界允许重叠，最终按规范化项目URL去重，避免金额小数造成遗漏。预检取得的第1页JSON直接写入子批次断点，正式采集不会重复请求该页。

系统最多生成200个批次，最多递归12层。如果数值范围已经无法继续安全拆分，工单进入“需人工拆分”，不会继续请求约第201页。地区不会自动拆分，因为任意地区列表无法保证完整覆盖全球；此时由用户选择一组明确地区后重新建立工单。

年份仍是返回项目后的本地过滤条件，不能用来让服务端直接跳到2014年。

## 间隔设置

控制台提供以下方案：

- 稳妥：30～60秒
- 较快：15～30秒
- 测试：8～15秒
- 保守：60～120秒
- 自定义：最少8秒

间隔跨页面、批次、工单和进程重启累计。选择更短间隔只改变本地等待参数，不表示网站允许该频率，也不保证不会出现403或验证。大批量运行建议保持30～60秒，每次只调整一个变量。

## 403和验证

URL阶段使用可见Edge和持久浏览器配置。出现验证时需要用户在Edge窗口中手动完成；系统不会自动点击或绕过验证。浏览器关闭、验证超时、403或429都会保存断点并停止后续批次。

元数据阶段复用同一HTTP会话。出现验证时不重试当前URL，也不继续后续任务，避免一个失败导致整批连续请求。失败任务保存在数据库重试队列。

因此，“定时运行”不等于可以无人处理网站验证。需要人工验证的工单会暂停或失败，用户检查日志后再点“立即运行”。

## Excel结果

每轮输出一个工作簿：

- **URL清单**：规范化URL、项目ID、名称、年份、来源批次等。
- **批次状态**：批次标识、下一页、`total_hits`、停止原因和错误。
- **项目数据**：标题、金额、日期、状态、创建者等。
- **创作者**
- **协作者**
- **说明**

MySQL不可用时仍会输出URL清单和批次状态，并在“说明”中记录数据库未导出的原因。

## Windows长期运行

首次安装依赖：

```powershell
python -m pip install -r requirements-metadata.txt
```

手动启动：

```powershell
.\start_control_panel.ps1
```

需要登录Windows后自动恢复队列时，在PowerShell中执行一次：

```powershell
.\install_windows_startup.ps1
```

该脚本注册当前用户的登录启动任务，控制台后台运行在 `http://127.0.0.1:8765`。电脑必须保持开机且用户已登录，Edge人工验证才可见。程序崩溃时Windows任务会尝试重启3次；工单本身由SQLite恢复。

## 使用边界

Kickstarter现行条款限制使用软件抓取网站并禁止不合理负载。运行批量工单前应确认已获得网站许可或使用获准的数据来源。自动拆分用于把获准查询控制在结果窗口内，不会请求第201页或绕过访问控制。
