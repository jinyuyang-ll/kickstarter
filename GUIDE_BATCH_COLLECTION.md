# 分批采集控制台使用指南

## Cloudflare 验证后的会话延续与冷却（2026-09-13）

学弟最新日志显示：上一任务的 G12 / G13 均成功，约 17 分钟后的新任务却在第一个请求 G12 第 6 页收到 HTTP 403；响应头为 `cf-mitigated: challenge`，标题为 `Just a moment...`。这证明本次是 Cloudflare 验证页，和选择了 5 个条件组、年份过滤或分页上限无关。

本版据此增加以下处理：

- `cf-mitigated: challenge` 和 `Just a moment...` 明确归类为验证页，不再只显示普通 403。
- 共享模式在任务内复用连接；每次成功获得 Discover JSON 后，只把脚本自身收到的 Kickstarter 域 Cookie 保存到 `.collection/session_state/`，下一任务恢复这些 Cookie。不会读取或导入浏览器 Cookie，也无法跨进程保留原 TCP / TLS 连接。
- 明确验证页即使没有 `Retry-After`，也会启动 1 小时本地安全冷却。冷却期间再次提交只读取断点并提示最早检查时间，不发送网络请求。1 小时是防止连续重试的本地保护，不代表网站届时一定恢复。
- 诊断现在同时记录实际页数、数量上限、间隔范围、Cookie 状态是否恢复及 Cookie 数量，只记录数量和状态，不记录 Cookie 名称或值。

`.collection/session_state/` 含本机站点会话 Cookie，已被 Git 忽略。不要上传、发送或复制到其他电脑；发给学长时只发送控制台下载的脱敏诊断日志。程序代理配置不同时使用不同状态文件，但程序无法识别系统 VPN 切换，因此采集期间仍应保持网络环境不变。

这些改动用于延续正常会话并阻止误操作，不能绕过 Cloudflare 验证，也不保证消除 403。若冷却结束后浏览器直接打开同一 JSON 地址仍显示验证页面，应继续暂停；若浏览器能返回 JSON 而脚本持续收到验证页，再评估经网站允许的浏览器式采集或其他数据来源。
## 403 排查与指定批次续跑（2026-09-13）

本次完成三项改动：每次实际请求输出脱敏诊断；可保留全部条件组而只运行勾选批次；默认整轮共用一个 HTTP 会话，并保留每批新建会话的对照模式。它们帮助定位和减少不必要的连接变化，**不代表网站 403 已解决**。

学弟下一次建议这样操作：

1. 备份原汇总 CSV 和同名 `.batches` 文件夹，更新代码并重启控制台。保留原 36 组、Games 类别、年份和输出文件名，无需删除 G1～G11。
2. 点击 **预览并选择批次** → **取消全部勾选** → 只勾选原 G12，确认“本轮执行范围”为“只运行下方勾选批次”。程序会使用原 G12 断点；若其上次失败于第 2 页，就从该页续跑。
3. 保持同一网络环境，选择默认 **整轮复用会话**，设置每批 1 页、最多新增 3 条、请求间隔 30～60 秒，只提交一次。年份不符导致新增 0 条仍可能是正常结果；日志会区分网络请求和页内缓存续跑。
4. 无论成功还是失败，都点击任务中的 **下载完整诊断日志**。补充当时 VPN 是否开启、是否刚切换网络及浏览器对应页面是否正常，不需要发配置文件、密码或 Cookie。若再次 403 / 验证，停止，不继续切组或反复点击。
5. 先根据该日志判断下一步，再决定是否恢复少量后续批次。**每批新建会话（仅对照测试）** 留给需要时的受控比较，不要求连续运行两种模式；仅单组单页无法判断跨批次复用效果。

诊断包含 UTC 请求时间、耗时、脱敏请求地址、HTTP 状态、响应类型、错误页标题和固定错误提示、部分响应标识、会话编号，以及程序代理是否配置 / 代理环境变量名称。只保留允许的响应头，不保存完整 HTML、Cookie 或密码。`vpn_or_system_route=not_detected` 表示程序无法判断 VPN 或系统路由，**不表示 VPN 已关闭**；响应头也不能单独证明拦截来源。

完整日志保存于 `.collection/job_logs/<任务编号>.txt`，不受界面日志截断影响。重启后任务列表可能清空，磁盘文件仍在，可直接发送对应文件或使用已保存的下载地址。日志与本地配置、采集结果一样由 Git 忽略，不公开提交。

两种模式在一轮中都固定使用同一个程序代理配置；共享模式在本轮复用连接，并跨任务保存脚本自身收到的 Kickstarter Cookie；每批新建模式不保存该状态。程序不导入浏览器 Cookie，网站仍可关闭连接或拒绝请求，也无法控制 VPN 或代理服务商在外部切换出口。遇到 403 / 429 / 验证仍保留断点并暂停后续所选批次。

本次不改变查询标识，`eef557b` 的分批断点可继续使用。勾选范围与连接模式不会改变查询身份；浏览器会保存勾选范围。汇总 CSV 现在保留原有 URL，即使只选一组或移除一组也不会缩小已有汇总。若要建立另一份限定范围的数据集，请使用新输出文件名。

## G13 日志反馈修复（2026-09-11）

- Games (`category_id=12`) 和 2014～2025 年筛选无需改变。返回 2026 年项目但保存 0 条是正常过滤，不代表请求失败。
- 新日志分别统计年份不符、年份缺失、URL 无效、重复和当前页尚未处理条目，并显示实际年份分布。控制台也显示最近处理页的这些计数。
- 有效项目 JSON 优先解析；项目名称或描述含 `captcha` / `security check` 不会再被直接认定为验证。HTTP 403、429、HTML 验证页和异常响应分别报告；错误保留 HTTP 状态、响应类型，不保存 Cookie 或验证页面正文。
- “请求间隔”在实际 Discover 请求前执行，跨页、跨条件组、跨任务和进程重启共用 `.collection/request_gate.sqlite3`。单组单页任务再次运行也会等待剩余间隔。只处理缓存的页内剩余项目或跳过已结束批次不发送请求，不额外等待。
- 新控制台默认 30～60 秒，仍可调整；这是本地保守试跑参数，不是官网保证的安全频率。间隔按请求开始时间计算，已过去的时间会扣除。旧命令行 / 保存计划中明确指定的间隔继续按原值执行。
- 遇到 403、429 或验证页仍暂停后续批次，不自动反复尝试。若响应提供 `Retry-After`，保存最早重试时间；该时间之前再次点击运行会提示暂停，不发送请求。时间过去也不自动恢复，需要检查访问状态后手动运行。没有该响应头时，普通 403 不推测恢复时间；明确 Cloudflare 验证页使用 1 小时本地安全冷却，但不承诺网站届时恢复。
- 本次保持查询标识和 CSV / 断点格式兼容上一版分批控制台，原 G13 查询标识仍为 `9cea05a2f52cc401`。更新代码并重启后，保持原输出文件、条件和年份可续跑。下文“旧版断点不兼容”指更早的、尚未加入年份标识的版本。

建议先保留原文件备份，在原计划中把间隔改为 30～60 秒、小批量续跑；日志中的年份跳过是正常现象，若出现明确 HTTP 错误则暂停排查。不要清除请求间隔文件来反复试探访问限制。

`Retry-After` 支持秒数与 HTTP 日期格式，依据 [HTTP 标准](https://www.rfc-editor.org/rfc/rfc9110.html#section-10.2.3)。


本版增加地区搜索、目标金额、已筹金额、已筹款比例、自定义条件组和独立断点。金额上下限与地区参数已对照 Kickstarter 官方前端并进行了少量实际接口验证。

## 更新和启动

已使用 Git 克隆的项目可以在目录中执行 `git pull --ff-only`，然后重启控制台。首次安装参考 [README](README.md)。不要覆盖已有 `config.json`。

```powershell
.\.venv\Scripts\python.exe web_app.py
```

浏览器打开 `http://127.0.0.1:8765`。虚拟环境无法运行时，应在自己的机器重新创建环境并安装 `requirements-metadata.txt`，不要复制别人的虚拟环境。

## 从 Tabletop Games 开始

1. 直接选择 `Games / Tabletop Games`。也可粘贴官网搜索链接，再点击 **读取链接筛选**；只粘贴而不读取时，以表单里的条件为准。
2. 选择状态。勾选 Successful 和 Failed 会创建两个独立查询，不再把两个状态合并翻页。
3. 需要地区时输入英文国家、州或城市名称，点击 **搜索地区**，从返回的名称中选择。`United States` 指整个美国；`California` 指地区结果中的州；同名城市需要核对显示的州和国家。没有选择时为全球。
4. 设置目标金额 / 已筹金额 / 筹款比例的下限和上限。金额单位按官网搜索界面为 **USD**；比例填 **100 表示 100%**。留空为不限，支持只填下限或只填上限。
5. 填写备注，例如“美国桌游，目标 1000–5000”，点击 **添加当前条件到批次**。
6. 修改地区或范围，继续添加其他条件组。每组内的多选状态仍会自动拆开。完全相同的查询自动合并。
7. 选择新的汇总 CSV，例如 `tabletop_urls.csv`，点击 **预览并选择批次**，核对类别、地区、状态、范围及年份。预览不发送采集请求。
8. 设置每批本轮最多页数和新增数量，例如先用 `1 页 / 3 条`；点击 **按所选范围运行 / 续跑**。所有批次串行执行，控制台强制至少 8 秒实际搜索请求间隔，新表单默认 30～60 秒。
9. 检查每批停止原因；确认结果后再增加每批本轮页数和数量，使用相同输出文件继续运行。数量填 `0` 表示只受本轮页数约束。
10. 汇总 CSV 中 URL 已规范化并去重，可以在“检查与导入”区域先执行 Dry-run，再决定正式导入 MySQL。

**已经添加条件组后，修改表单不会修改队列中的已有组。** 需要再次添加，并移除不再需要的组。只运行列出的条件组；没有添加任何组时，运行当前表单。

## 条件如何拆分

例如在某个地区、状态下，把目标金额拆为下列条件组：

| 下限 USD | 上限 USD |
| --- | --- |
| 留空 | 1000 |
| 1000 | 5000 |
| 5000 | 10000 |
| 10000 | 留空 |

这些只是拆分示例，**不是保证每批小于分页限制的固定方案**。若某组仍分页受限，可以继续用更窄的目标金额范围、已筹金额、比例或更具体的地区拆分。各维度在同一组内是同时满足的条件。

相邻区间使用相同边界即可，不要为了避重手动加减 1，避免遗漏小数金额；本地汇总负责去重。官网金额换算与边界行为以实际接口为准，本次美元项目样本的组合范围验证通过，未穷尽所有币种和精确边界。

用城市或部分国家划分并不自动覆盖全球。Failed 项目若同时限制“比例至少 100%”，可能没有结果；这不等于所有失败项目已采完。不要无意间把排除历史项目的条件组合在一起。

## 停止状态

| 状态 | 含义 / 下一步 |
| --- | --- |
| 本查询结果已结束 | 接口返回没有更多结果；仅代表本查询，不代表全站历史全量 |
| 已翻过目标年份 | Newest 排序下连续翻过目标年份，停止本查询 |
| 本轮页数用完，可续跑 | 本次设置的请求页数已用完，继续运行相同条件 |
| 本轮数量用完，可续跑 | 新增数量达标；当前页未处理记录已保存在断点中 |
| 疑似分页受限，请拆细条件 | 上页成功而下一页 404；保留数据，不反复重试该页，应拆细查询 |
| 请求失败，进度已保存 | 网络、403/429 或响应结构异常；错误类型单独显示，后续批次暂停，排查后继续 |

界面黄色“待继续 / 待拆分”不是程序崩溃，也不是采集完成。采集脚本退出码 `0` 为查询结束、`3` 为本轮结束但尚未完成、`2` 为请求或配置失败。

年份是按项目 `launched_at` **本地筛选**，不会把服务端查询直接定位到指定年份。断点续跑也无法使同一查询穿过服务端的分页限制。

## 文件与恢复

- `tabletop_urls.csv`：已有 URL 与本次计划各批次结果的去重汇总。
- `tabletop_urls.batches/<查询标识>.csv`：每个查询独立的结果。
- 同目录中的 `.checkpoint.json`：页码、年份、停止原因及当前页未处理记录。
- `tabletop_urls.batches/manifest.json`：最近一次运行的各批次状态。
- `.collection/*.json`：每次提交的完整计划。
- 浏览器在当前站点的本地存储中保存条件组与输出文件名；更换浏览器、端口或清理站点数据后需重新添加条件组。使用相同条件、年份和输出位置仍可匹配磁盘断点。

已结束的查询在续跑时跳过；改变金额、地区、状态或年份会建立新查询断点。修改每轮页数或数量不影响原断点。若要重新扫描已结束的查询以获取新上线项目，请换一个新的输出文件名建立新批次。

只选部分批次或移除组后，已有汇总 URL 和原组的独立文件仍保留。请不要手动删除或编辑正在使用的 CSV / 断点。只支持同一控制台中一个采集任务同时运行，不要用多个控制台或命令行进程同时写同一输出文件。

旧版断点不含完整的年份标识和页内剩余记录，**不直接迁移旧断点**。请新建输出文件运行新版。旧 CSV 保留不动；旧版已跳过的 URL 只能重新扫描相关查询补回。

## 命令行示例

```powershell
.\.venv\Scripts\python.exe url_collector.py `
  --category-id 34 --state successful --sort newest `
  --woe-id 23424977 `
  --goal-min 1000 --goal-max 5000 `
  --pledged-min 1000 --pledged-max 20000 `
  --raised-min 100 --raised-max 400 `
  --max-pages 1 --max-projects 3 `
  --output tabletop_us_sample.csv --resume
```

命令行单查询保留多状态合并能力；需要自动分状态和统一汇总时使用控制台，或运行控制台生成的 `--plan-file`。表单/显式命令行参数覆盖链接中的对应筛选；未指定的命令行参数继承链接。

## 验证资料

- [Kickstarter 官方高级搜索说明](https://help.kickstarter.com/hc/en-us/articles/115005066253-How-does-Advanced-Search-work)
- [官方 Tabletop Games 搜索页面](https://www.kickstarter.com/discover/advanced?category_id=34&sort=newest)
- [本版内测记录](TEST_REPORT_COLLECTION.md)
